---

- name: openvpn restart
  service: name={{openvpn_service}} state=restarted

- name: openvpn pack clients
  command: "{{ openvpn_etcdir }}/build-client-config.sh {{ item.item }}"
  when: item.changed
  with_items: "{{openvpn_clients_changed.results}}"
